{
  "extends": "../../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": "..",
    "paths": {
      // When running tests, use TS files, this allows us to not require to build
      "@synthetixio/hardhat-router": ["src"]
    }
  }
}
