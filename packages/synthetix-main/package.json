{
  "name": "@synthetixio/main",
  "version": "1.0.0",
  "description": "",
  "files": [
    "contracts",
    "!contracts/mocks"
  ],
  "scripts": {
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "compile-contracts": "hardhat compile",
    "size-contracts": "hardhat size-contracts",
    "slither-local": "python3 -m venv .venv && .venv/bin/python -m pip install slither-analyzer && .venv/bin/python -m slither .",
    "slither-sarif": "python3 -m venv .venv && .venv/bin/python -m pip install slither-analyzer && .venv/bin/python -m slither . --sarif slither.sarif"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "2.0.2",
    "hardhat": "2.6.8",
    "hardhat-contract-sizer": "2.1.1",
    "solidity-coverage": "0.7.17"
  }
}
