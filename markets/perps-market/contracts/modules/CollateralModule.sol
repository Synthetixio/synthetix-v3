contract CollateralModule {
    modifyCollateral(uint128 accountId, address collateralType, uint amount){
        // RBAC check for permission of msg.sender for account id

        // currentAmount = check collateral associated with the account

        // if collateralType == snxUSD token
            // if greater
            // else
        // else
            // if greater
            // else

        // record change
        
        // emit event
    }
}