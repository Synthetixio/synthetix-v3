/*
function commitBuyOrder(marketId, usdAmount) returns (nftID) {
    // transfer in usd amount
    // issues an NFT
}

function commitSellOrder(marketId, synthAmount) returns (nftID) {
    // transfer in synth amount
    // issues an NFT
}

function settleOrder(marketId, nftId, priceData){
    // confirms data
    // settles trade
    // burns NFT
}
*/