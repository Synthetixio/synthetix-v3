name: Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package:
          [
            "@synthetixio/core-contracts",
            "@synthetixio/core-utils",
            "@synthetixio/core-modules",
            "@synthetixio/hardhat-router",
            "@synthetixio/sample-project",
            "@synthetixio/synthetix",
          ]
        include:
          - package: "@synthetixio/core-contracts"
            coverage-file: utils/core-contracts/coverage.json
          - package: "@synthetixio/core-utils"
            coverage-file: utils/core-utils/coverage/lcov.info
          - package: "@synthetixio/core-modules"
            coverage-file: utils/core-modules/coverage.json
          - package: "@synthetixio/hardhat-router"
            coverage-file: utils/hardhat-router/coverage/lcov.info
    steps:
      - name: Install Foundry (Cannon)
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: nightly
      - run: anvil -V
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: "16.16.0"
          cache: "npm"
      - run: npm ci
      - run: npm run build
      - name: Execute tests
        run: REPORT_GAS=true npm run --workspace=${{ matrix.package }} coverage
      - if: ${{ matrix.coverage-file }}
        name: Upload ${{ matrix.package }} coverage to Codecov
        uses: codecov/codecov-action@v2
        with:
          files: ${{ matrix.coverage-file }}
          flags: ${{ matrix.package }}
