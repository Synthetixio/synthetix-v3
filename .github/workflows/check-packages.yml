name: "Packages dependencies definitions"

on: [push, pull_request]

jobs:
  core-js:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v1
      - name: Check file existence
        id: check_files
        uses: andstor/file-existence-action@v1
        with:
          files: "packages/core-js/package-lock.json"
      - name: File exists
        if: steps.check_files.outputs.files_exists == 'true'
        run: echo 'Should not add package-lock.jso file to a package.'; exit 1;
  deployer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v1
      - name: Check file existence
        id: check_files
        uses: andstor/file-existence-action@v1
        with:
          files: "packages/deployer/package-lock.json"
      - name: File exists
        if: steps.check_files.outputs.files_exists == 'true'
        run: echo 'Should not add package-lock.jso file to a package.'; exit 1;
  synthetix-core:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v1
      - name: Check file existence
        id: check_files
        uses: andstor/file-existence-action@v1
        with:
          files: "packages/synthetix-core/package-lock.json"
      - name: File exists
        if: steps.check_files.outputs.files_exists == 'true'
        run: echo 'Should not add package-lock.jso file to a package.'; exit 1;
