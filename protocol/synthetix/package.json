{
  "name": "@synthetixio/main",
  "version": "3.0.1",
  "description": "Core Synthetix Protocol Contracts",
  "private": true,
  "scripts": {
    "clean": "hardhat clean",
    "build": "rm -rf contracts/generated && hardhat storage:verify && hardhat generate-testable && hardhat compile",
    "cannon": "npx hardhat cannon:build",
    "test": "npm run build && hardhat test",
    "coverage": "npm run build && hardhat coverage --network hardhat",
    "compile-contracts": "hardhat compile",
    "size-contracts": "hardhat compile && hardhat size-contracts"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@synthetixio/common-config": "1.0.0",
    "@synthetixio/core-contracts": "1.1.1",
    "@synthetixio/core-modules": "1.0.0",
    "@synthetixio/oracle-manager": "1.2.10",
    "solidity-docgen": "0.6.0-beta.22",
    "ts-node": "^10.9.1"
  }
}
